Lots of constants (ALL UPPERCASE like Ruby) can monkey with an app. Namespace it and use a global variable to hold all variables.

* EX: MYAPP.stooge.firstname
* `MYAPP.stooge{};` and `MYAPP.fligh{};`
