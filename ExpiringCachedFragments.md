* `expire_fragment(action => 'actionName', :part => 'partToBeExpired')`
  * These would be placed in the controller actions that require them
* These would be placed in the controller actions that require them